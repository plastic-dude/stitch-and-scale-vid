# First Director Product-Proof Observations

**Inspected:** 2026-08-19
**Source commit:** local checkout `plastic-dude/stitch-and-scale-pro` at the current verified repository state.

## `docs/screenshots/spec-sheet-lab.webp`

The current repository screenshot shows Stitch & Scale’s `Spec Sheet Lab` for “Demo Crewneck Sweater.” The shell visibly says `Local only`, and the project header shows gauge `18sts x 24rows / 4in`. The visible surface includes `Full Grading Table` and `Export PDF` actions. The main lab shows a `Quote-readiness` blocked state, POM points, a yarn bill marked `Complete`, tolerance band `±0.25in`, a gauge and construction block, and a Points of Measure table with size columns XS through 5XL. Visible rows include Body > Bust Circumference, Body > Back Length, Body > Waist Circumference, Sleeve > Upper Arm Circumference, and Sleeve > Sleeve Length.

**Safe use:** This is a strong approved repository proof candidate for a focused size/measurement/quote-readiness story if the Generator uses this exact repository asset and the current commit is recorded. Do not claim that the screenshot proves all product capabilities, that all values are mathematically correct, or that quote-readiness is complete; the screenshot visibly shows a blocked quote-readiness state.

## `docs/screenshots/storage-health-card.webp`

The screenshot shows Settings/storage proof with `Export Workspace`, `Download Backup`, `Restore from Backup`, and `Upload File`. The `Storage Health` card visibly reports Browser storage `1.0 KB`, Offline cache `1.0 KB`, `Stores in sync`, `Reconcile`, and `Never backed up`; it states that the workspace keeps two independent copies on the device and that a backup file is the only true insurance outside the browser. The shell visibly says `Local only`.

**Safe use:** This is a strong repository proof candidate for a local-first ownership or recoverability story. The video must accurately describe it as a visible current surface, not promise perfect data safety or imply that a backup already exists. The Generator should show the actual capture without generated text and keep captions away from the storage cards and buttons.

## Production implication

The first `spreadsheet-11pm` campaign can use `spec-sheet-lab.webp` as the primary proof surface, with one close shot of the size table and one close shot of the blocked/quote-readiness state. A later distinct `privacy-by-default` campaign can use `storage-health-card.webp`. The two screenshots should not be mixed into one first brief unless the Director explicitly changes the campaign objective and records a new angle decision.
