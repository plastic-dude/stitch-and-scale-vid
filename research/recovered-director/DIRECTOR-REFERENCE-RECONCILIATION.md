# Director Reference Reconciliation

## Imported sources

Two attached reference tasks were imported from the session-reference system:

| Reference | Task ID | Conversation events | Attached files |
|---|---|---:|---:|
| Director/Planner Responsibilities Outline | `G6tqH2yPtwmxj7CxqzhqT5` | 60 | 15 |
| Director/Planner Temporary Role Outline | `f8KLv72SAgR2v2dPGg53bY` | 70 | 20 |

The full visible conversation records are preserved as `conversation.jsonl` under each task directory. The complete attached file contents are preserved in each `files/` directory and concatenated into `DIRECTOR-REFERENCE-MERGED.md`.

## Findings that affect the current operation

Both referenced tasks describe the same temporary Director/Planner contract: fresh research on every firing, sequential reading of the relevant Director/video documents, repository and blocker inspection, one complete brief or one precise blocker per run, distinct angle selection, complete claim/platform/caption/audio/rights/filename records, durable communication, and no video generation or self-approval by the Director.

The referenced task replay confirms that the schedule was configured with `runAsNewTask: true` and that a new scheduled task was created from the parent Director session. Therefore, the current seven-minute Director schedule is intentionally a fresh-chat schedule rather than a continuation of this conversation. This is consistent with the current active schedule state and is not a reuse of the current chat.

The imported materials also confirm the operational failure mode that caused the Generator to remain blocked: the archive-connectivity test was treated as a reviewable file without a complete production identity and without the matching brief, manifest, platform profile, claim ledger, caption map, audio plan, rights record, checksum, and acknowledgement. The correct recovery is to preserve the test file and create a separate, fully identified production brief.

The imported research and briefs reinforce two production priorities: first, a proof-led stale-number or spreadsheet-labor concept must use one readable, current product proof surface rather than a dense generic feature tour; second, caption safety, voice continuity, rights, platform profile, and mobile readability must be declared before generation. The imported vertical-proof research should be treated as a platform-adaptation input, not as permission to reuse a 16:9 asset without a new crop, caption map, manifest, and QA record.

## Current application

The active temporary Director schedule remains the authoritative trigger. The current repository handoff is `PLAN-20260819-spreadsheet-11pm-001`, committed and published after remote integration. Any newly recovered Director artifact must be reconciled against that brief, the current angle reservations, the latest remote commit, and any newer Generator or Reviewer message before changing the queue.

## Source-preservation rule

The merged reference document is a preservation copy. It must not be silently edited to make the historical records agree with current plans. New decisions belong in a new dated research note or run report, with the source reference named.
